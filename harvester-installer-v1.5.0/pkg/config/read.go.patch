--- read.go.ORIG	2025-04-29 13:59:52.000000000 -0700
+++ read.go	2025-04-29 14:02:20.000000000 -0700
@@ -14,6 +14,7 @@
 const (
 	kernelParamPrefix   = "harvester"
 	defaultUserDataFile = "/oem/userdata.yaml"
+	cloudConfigFile = "/var/tmp/harvester-cloudconfig.yaml"
 )
 
 // ReadConfig constructs a config by reading various sources
@@ -55,6 +56,22 @@
 	return readUserData(defaultUserDataFile)
 }
 
+// IsCloudConfig checks if cloud-config generated by cloud-init nocloud exists
+func IsCloudConfig() (bool, error) {
+	_, err := os.Stat(cloudConfigFile)
+	if os.IsNotExist(err) {
+		return false, nil
+	} else if err != nil {
+		return false, err
+	}
+	return true, nil
+}
+
+// ReadCloudConfig constructs a config from cloud-config generated by cloud-init nocloud
+func ReadCloudConfig() (HarvesterConfig, error) {
+	return readUserData(cloudConfigFile)
+}
+
 func readUserData(fileName string) (HarvesterConfig, error) {
 	result := NewHarvesterConfig()
 	_, err := os.Stat(fileName)
